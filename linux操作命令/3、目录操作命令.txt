mkdir  目录名   			新建一个目录

mkdir -p 目录/目录/目录... 	创建多级目录 

ls 		查看当前目录下所有目录和文件

ls -a	查看所有目录和文件，包括隐藏

ll		查看目录和文件的列表显示（包括权限信息）

find 目录；	查找，显示，所有与目录相关的文件、目录

mv oldFileName newFileName ；	修改目录名字（重命名）。各种文件压缩包有效

mv file /aaa;	移动、剪切目录到aaa目录下（文件压缩包）

rm -r     删除目录（询问是否确定删除）
rm -rf		删除 ， 不询问

